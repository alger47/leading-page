# CHANGELOG - Project Changes Documentation

## Date: 2026-09-09

### Phase 0 — Protocol Bootstrap
- Read and acknowledged master prompt (GIDE.txt)
- Confirmed 12 Prime Directives
- Created docs/MASTER_PROMPT.md

### Phase 1 — Deep Discovery
- Analyzed repository structure (greenfield project)
- Created docs/discovery-report.md (sections A-M)
- Created docs/architecture.md
- Identified technology stack and architecture decisions

### Phase 2 — Page Schema Foundation
**Created:**
- ADR-0002: Canonical Schema Strategy (JSON Schema → Zod + Pydantic)
- Root config: pnpm-workspace.yaml, turbo.json, tsconfig.base.json, package.json
- packages/page-schema/schema/envelope.schema.json (main document envelope)
- packages/page-schema/schema/sections/hero.schema.json
- packages/page-schema/schema/sections/features.schema.json
- packages/page-schema/schema/sections/cta.schema.json
- packages/page-schema/schema/sections/header.schema.json
- packages/page-schema/schema/sections/footer.schema.json
- packages/page-schema/src/validators/structural.ts (L1 validator)
- packages/page-schema/src/validators/semantic.ts (L2 validator with SEM-001..004)
- packages/page-schema/src/registry.ts (section type registry)
- packages/page-schema/src/index.ts (package exports)
- packages/page-schema/src/migrations/index.ts (migration skeleton)
- packages/page-schema/examples/valid-vet-ar-001.json (Arabic RTL fixture)
- packages/page-schema/examples/valid-saas-en-001.json (English LTR fixture)
- packages/page-schema/examples/invalid-no-hero.json (missing hero)
- packages/page-schema/examples/invalid-duplicate-ids.json (duplicate IDs)
- packages/page-schema/tests/structural.test.ts
- packages/page-schema/tests/semantic.test.ts

**Decisions:**
- Canonical JSON Schema as source of truth
- Codegen to Zod (TypeScript) and Pydantic (Python)
- CI drift check for generated artifacts

**Awaiting:** "continue" → Phase 3 — Design System + Renderer

### Phase 3 — Design System + Renderer (2026-09-09)
**Created:**
- packages/design-system (tokens, 4 theme presets, typography w/ Arabic-first, RTL helpers)
- packages/ui-components (ThemeProvider, primitives, 5 sections, registry, Renderer)
- packages/ui-components/vitest.config.ts + tests/setup.ts (jsdom cleanup)
- Tests: design-system (9) + ui-components (23: sections/renderer/matrix)

**Fixed:**
- Vitest alias path with spaces (LEADING PAGE) → fileURLToPath
- Renderer brand-name queries (header+footer duplicate) → getAllByText
- Registry typing + tsconfig paths to design-system source
- Features grid honors layoutHint.columns

**Status:** All package tests pass (page-schema 15, design-system 9, ui-components 23 = 47 total); typecheck clean; codegen drift check green.

**Awaiting:** "continue" → Phase 4 — AI Engine

### Phase 4 — AI Engine (2026-09-09)
**Created** (`apps/ai-engine`, FastAPI + Python 3.12):
- API: `app/api/{container,deps,routes}.py`, `app/main.py` (healthz public; `/internal/v1/generate`, `/ledger/{job_id}`, `/prompts` behind X-Internal-Token)
- Provider abstraction (PART VI §6.2): `app/providers/{protocol,stub,stub_data,http_providers,factory}.py`; stub is deterministic and never echoes brief text (anti-hallucination/injection by construction)
- Config-driven routing (§6.3): `config/routing.yaml`, `app/routing/config.py`
- Versioned prompts (§6.6): 5 YAML assets (stage1..stage5) + `app/prompts/{assets,registry}.py`; canonical stage JSON Schemas in `app/schemas/` (ADR-0002)
- Repair ladder (§6.5): `app/services/{stage_runner,repair,fallbacks,validate,pipeline}.py` — retry → targeted re-ask → deterministic safe repair → honest `E-AI-004`; provider crashes → one recorded attempt then `E-AI-005`
- Cost ledger (§6.9): `app/cost/ledger.py` — per-attempt USD estimates, per-job attempt cap (`E-AI-003`) + budget (`E-AI-002`)
- L0 gate (§8.1): `app/core/brief_validation.py` — length caps, ar/fr/en locale detection, injection flagging
- Error codes E-AI-001..006: `app/core/errors.py`
- Mini-eval (§9.1–9.2): 13 golden briefs (12 verticals × {ar,fr,en} + injection case) in `evaluation/golden/`, `app/evaluation/mini_eval.py`, committed report `evaluation/reports/phase4-mini-eval.md`

**Fixed during bring-up:**
- `PromptAsset.render` → slot-only regex substitution (literal braces in prompt prose no longer raise KeyError)
- SchemaStore directory resolution, stub plan shape (slots as object, hero-first/footer-last) to satisfy schema + SEM rules
- Locale detection: accent-aware French heuristic; broader injection phrasing (ignore these instructions, set ... instead)

**Docs:** `.env.example`, `README.md`, `docs/ai-pipeline.md`, ADR-0003.

**Status:** 41 pytest green; ruff clean; mypy clean (32 modules); mini-eval 13/13 cases, validity 1.0 ≥ 0.99, per-stage attempts/cost recorded, injection case flagged and artifact-free.

**Awaiting:** "continue" → Phase 5 — SchemaBuilder (assemble stage outputs into the Page Schema)

### Phase 5 — SchemaBuilder (stage 7) + Page Schema preview (2026-09-09)
**Created:**
- `app/services/schema_builder.py` — deterministic stage 7 assembly (ordering anchors header/hero/footer, slot mapping image→media, null optional CTA slots dropped, locale direction/font, theme defaults, honest page title); diagnostics E-BUILD-001..003
- `app/services/page_validator.py` — L1 against the CANONICAL `packages/page-schema/schema/envelope.schema.json` (ADR-0002, `AI_PAGE_SCHEMA_DIR`, E-BUILD-004) + Python SEM-001..004 mirror; `page_validation` rides on every completed job
- `app/evaluation/export_fixtures.py` (+ `--check` drift guard) → `packages/page-schema/examples/ai-vet-ar-001.json`, `ai-saas-en-001.json`
- `packages/ui-components/tests/ai-fixture.test.tsx` (schema→DOM pin, §6.12); page-schema TS validators assert L1+SEM on AI fixtures
- API `GET /internal/v1/pages/{job_id}`; `JobResult.page/page_validation/build_issues`
- Mini-eval page validity: 13/13 assembled pages valid (pages_validity_rate ≥ 0.99), report `evaluation/reports/phase5-mini-eval.md`

**Status:** 66 pytest green; ruff clean; mypy clean (35 modules); TS ui-components 33 green (incl. ai-fixture) + page-schema 19 green; fixture export byte-deterministic (drift check green); mini-eval validity 1.0 & pages_validity 1.0.

**Awaiting:** "continue" → Phase 6 — Worker & job orchestration (master catalog) / Database & persistence per PART X
