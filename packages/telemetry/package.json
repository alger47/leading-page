{
  "name": "@landing-ai/telemetry",
  "version": "1.0.0",
  "private": true,
  "description": "Structured logging, in-process spans, and metrics counters for the landing-ai platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}