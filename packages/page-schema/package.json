{
  "name": "@landing-ai/page-schema",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical Page Schema - the single contract for all subsystems",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "codegen:check": "node scripts/codegen-check.mjs"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "json-schema-to-zod": "^2.0.0"
  }
}
