{
  "name": "@landing-ai/page-schema",
  "version": "1.0.0",
  "private": true,
  "description": "Page Schema tests",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "@landing-ai/page-schema": "workspace:*"
  }
}
